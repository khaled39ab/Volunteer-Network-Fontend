import React from 'react';
import useEvents from '../../hooks/useEvents';

const User = () => {

    return (
        <div>
            <h1>Your Event</h1>
        </div>
    );
};

export default User;